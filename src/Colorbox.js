
export function Colorbox({ color }) {
  const styles = {
    marginTop: "10px",
    width: "250px",
    height: "25px",
    backgroundColor: color
  };
  return (
    <div style={styles}>

    </div>
  );
}
